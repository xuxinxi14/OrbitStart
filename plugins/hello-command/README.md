# Hello Command

Minimal OrbitStart plugin template.

The current app discovers `plugin.json` and exposes a manifest-driven command in
the command palette. Full isolated TypeScript execution is planned behind the
same manifest contract.
